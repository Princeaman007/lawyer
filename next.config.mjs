/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "jurislex.fr",
    output: "export",
    reactStrictMode: true,
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
